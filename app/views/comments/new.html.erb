<div class="container-fluid">
    <div class="row">
        <div class="col-md-12 mt-3">
            <div class="jumbotron">
                    <fieldset>
                        <h1><%= "#{@article.title}" %></h1>
                            <%= render 'form', comment: @comment %>
                    </fieldset>
            </div>
        </div>
    </div>
</div>

